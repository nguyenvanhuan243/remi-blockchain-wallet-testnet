import { IChainTransaction } from '../types';
export declare class ChainTransaction implements IChainTransaction {
    hash: string;
    blockHash: string;
    blockNumber: number;
    destination: string;
    source: string;
    value: number;
    fee: number;
    constructor(hash: string, blockHash: string, blockNumber: number, destination: string, source: string, value: number, fee: number);
}
