"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.asignMasterKey = exports.recovery = exports.create = void 0;
const bip39 = __importStar(require("bip39"));
const uuid_1 = require("uuid");
const MasterKey_1 = require("../MasterKey");
const utils_1 = require("../utils");
/**
 * Create a new masterkey
 * @param name The masterkey's identify name
 * @param words The materkey's words count
 * @returns
 */
const create = (name, words) => __awaiter(void 0, void 0, void 0, function* () {
    const id = (0, uuid_1.v4)();
    const mnemonic = bip39.generateMnemonic((0, utils_1.wordsCountToStrength)(words));
    const seed = yield bip39.mnemonicToSeed(mnemonic);
    return new MasterKey_1.MasterKey(id, name, mnemonic, seed.toString('hex'));
});
exports.create = create;
const recovery = (name, mnemonic) => __awaiter(void 0, void 0, void 0, function* () {
    if (bip39.validateMnemonic(mnemonic)) {
        const id = (0, uuid_1.v4)();
        const seed = yield bip39.mnemonicToSeed(mnemonic);
        return new MasterKey_1.MasterKey(id, name, mnemonic, seed.toString('hex'));
    }
    return null;
});
exports.recovery = recovery;
const asignMasterKey = (obj) => {
    return new MasterKey_1.MasterKey(obj.id, obj.name, obj.mnemonic, obj.seed);
};
exports.asignMasterKey = asignMasterKey;
