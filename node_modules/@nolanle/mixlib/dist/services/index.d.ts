import { MasterKey } from '../MasterKey';
import { IMasterKey } from '../types';
/**
 * Create a new masterkey
 * @param name The masterkey's identify name
 * @param words The materkey's words count
 * @returns
 */
export declare const create: (name: string, words: 12 | 15 | 18 | 21 | 24) => Promise<MasterKey>;
export declare const recovery: (name: string, mnemonic: string) => Promise<MasterKey>;
export declare const asignMasterKey: (obj: IMasterKey) => MasterKey;
